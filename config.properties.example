# Copiar como "config.properties" (misma carpeta) y completar con las
# credenciales reales del servidor MySQL local.
db.url=jdbc:mysql://localhost:3306/vetgest_db?useSSL=false&serverTimezone=UTC
db.usuario=root
db.contrasenia=tu_contrasenia_aqui
